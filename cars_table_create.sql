CREATE TABLE IF NOT EXISTS autoscout.cars (
	mark VARCHAR(100),
	model VARCHAR(100),
	mileage INT,
	transmission VARCHAR(50),
	registration DATE,
	fuel VARCHAR(50),
	power INT,
	location VARCHAR(50),
	price INT
);